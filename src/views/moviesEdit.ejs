<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="titulo">Editar Película</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1 class="titulo">Película a Editar: <%= Movie.title %>
    </h1>
    <section class="formulario">
        <form action="/movies/update/<%= Movie.id %>?_method=PUT" method="POST" novalidate>
            <label for="title">Title </label>
            <p><input type="text" name="title" id="title" value="<%= locals.old && locals.old.title ? old.title : Movie.title %>" required>
            <!-- Posibles errores -->
                <span 
                  ><%= locals.errores && locals.errores.title ? errores.title.msg :
                  null %></span>
            </p>
            <label for="rating">Rating </label>
            <p><input type="number" name="rating" id="rating" value="<%= locals.old && locals.old.rating ? old.rating : Movie.rating %>" required>
            <!-- Posibles errores -->
                <span 
                  ><%= locals.errores && locals.errores.rating ? errores.rating.msg :
                  null %></span
                ></p>
            <label for="awards">Awards </label>
            <p><input type="number" name="awards" id="awards" value="<%= locals.old && locals.old.awards? old.awards: Movie.awards%>" required>
            <!-- Posibles errores -->
                <span 
                  ><%= locals.errores && locals.errores.awards ? errores.awards.msg :
                  null %></span
                >
            </p>
            <label for="release_date">Release date </label>
            <p><input type="date" name="release_date" id="release_date" value="<%= locals.old && locals.old.release_date ? old.release_date : fecha %>" required>
             <!-- Posibles errores -->
                <span 
                  ><%= locals.errores && locals.errores.release_date ? errores.release_date.msg :
                  null %></span
                >
            </p>
            <label for="length">Length </label>
            <p><input type="number" name="length" id="" value="<%= locals.old && locals.old.length ? old.length : Movie.length %>" required>
                 <!-- Posibles errores -->
                <span 
                  ><%= locals.errores && locals.errores.length ? errores.length.msg :
                  null %></span
                >
            </p>
            <br>
            <p> <button class="botonAgregar" type="submit">Actualizar</button>

                <a class="botonVolver" href="/movies">Listado de Películas</a>
            </p>
        </form>
    </section>
</body>

</html>